using UnityEngine;
using UnityEngine.UI;

public class GameUI : MonoBehaviour
{
    // public GameOfLife gameOfLife;
    // public Button startButton;

    // private bool isRunning = false;

    // void Start()
    // {
    //     startButton.onClick.AddListener(ToggleGame);
    // }

    // void ToggleGame()
    // {
    //     isRunning = !isRunning;
    //     if (isRunning)
    //     {
    //         gameOfLife.InvokeRepeating(nameof(GameOfLife.Update), 0.5f, 0.5f);
    //         startButton.GetComponentInChildren<Text>().text = "Pause";
    //     }
    //     else
    //     {
    //         gameOfLife.CancelInvoke(nameof(GameOfLife.Update));
    //         startButton.GetComponentInChildren<Text>().text = "Start";
    //     }
    // }
}